name: Publish build

on:
  push:
    branches:
      - master

jobs:
  publish:
    name: Upload build
    runs-on: ubuntu-latest
    if: contains(github.event.head_commit.message, '[ci skip]') == false

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"

      - name: Download & Install SlimefunTranslation API
        run: |
          mkdir -p libs
          curl -fsSL https://github.com/guizhanss/SlimefunTranslation/releases/download/v0.2.1/SlimefunTranslation-v0.2.1.jar -o libs/SlimefunTranslation.jar
          mvn install:install-file -Dfile=libs/SlimefunTranslation.jar -DgroupId=net.guizhanss -DartifactId=SlimefunTranslation -Dversion=v0.2.1 -Dpackaging=jar

      - name: Build with Maven
        run: mvn clean package

      - name: Upload to Blob Builds
        uses: WalshyDev/blob-builds/gh-action@main
        with:
          project: SlimeHUD
          apiToken: ${{ secrets.BLOB_BUILDS_API_TOKEN }}
          releaseNotes: ${{ github.event.head_commit.message }}
